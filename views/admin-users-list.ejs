<%- include('partials/head') %>
<%- include('partials/nav') %>

<main class="main-content">
  <section class="card">
    <div class="row-between">
      <h1>Usuarios</h1>
      <a href="<%= basePath %>/admin/users/new" class="btn btn-primary">Nuevo Usuario</a>
    </div>

    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Usuario</th>
            <th>Zona pagadora</th>
            <th>Rol</th>
            <th>Activo</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach((user) => { %>
            <tr>
              <td><%= user.id %></td>
              <td><%= user.name %></td>
              <td><%= user.username %></td>
              <td><%= user.zona_pagadora ?? 0 %></td>
              <td><span class="badge"><%= user.role %></span></td>
              <td><%= user.active ? 'Si' : 'No' %></td>
              <td>
                <a href="<%= basePath %>/admin/users/<%= user.id %>/edit" class="btn btn-ghost">Editar</a>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </section>
</main>

<%- include('partials/foot') %>
